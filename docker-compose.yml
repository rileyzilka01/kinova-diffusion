services:
  kinova_diffusion:
    container_name: kinova-diffusion
    working_dir: /home/user/kinova-diffusion
    build:
      context: .
      dockerfile: Dockerfile
      network: host
    network_mode: host
    tty: true
    privileged: true
    environment:
      - DISPLAY=${DISPLAY}
    devices:
      - /dev:/dev 
    volumes:
      - ../kinova-diffusion:/home/user/kinova-diffusion
      - /tmp/.X11-unix:/tmp/.X11-unix
      - /dev/bus/usb:/dev/bus/usb

    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
